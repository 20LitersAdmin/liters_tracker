-# frozen_string_literal: true
%p You should talk to an admin:
.card.my-2
  .card-body
    %h5.card-title System Admins:
    %p.card-text
      %table.table
        - @admins.each do |admin|
          %tr
            %td= admin.name 
            %td= link_to admin.email, 'mailto:' + admin.email
