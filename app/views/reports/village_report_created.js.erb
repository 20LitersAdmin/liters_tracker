// for sectors#report:_village_form
// clear out the form fields
$('input.form-control').val('')
$('select.form-control.optional').val('')
// destroy the existing DataTable
$("table#dttb_reports_villages").DataTable().destroy();
// re-populate the table body using the partial
$("table#dttb_reports_villages tbody").html("<%= j render partial: 'sectors/report', collection: @reports %>");
// re-init the DataTable
$("table#dttb_reports_villages").DataTable({
  order: [[ 0, 'asc' ], [ 1, 'asc' ]]
});
